/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  allowedDevOrigins: ["lauren-semirealistic-aurore.ngrok-free.dev", "*.ngrok-free.dev"]
};

export default nextConfig;
